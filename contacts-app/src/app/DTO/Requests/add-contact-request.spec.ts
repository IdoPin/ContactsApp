import { AddContactRequest } from './add-contact-request';

describe('AddContactRequest', () => {
  it('should create an instance', () => {
    expect(new AddContactRequest()).toBeTruthy();
  });
});
