import { UpdateContactRequest } from './update-contact-request';

describe('UpdateContactRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateContactRequest()).toBeTruthy();
  });
});
