import { DeleteGroupResponseOk } from './delete-group-response-ok';

describe('DeleteGroupResponseOk', () => {
  it('should create an instance', () => {
    expect(new DeleteGroupResponseOk()).toBeTruthy();
  });
});
