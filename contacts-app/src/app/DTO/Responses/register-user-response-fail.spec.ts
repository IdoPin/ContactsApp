import { RegisterUserResponseFail } from './register-user-response-fail';

describe('RegisterUserResponseFail', () => {
  it('should create an instance', () => {
    expect(new RegisterUserResponseFail()).toBeTruthy();
  });
});
