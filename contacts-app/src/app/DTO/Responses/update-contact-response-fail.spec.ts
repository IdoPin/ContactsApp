import { UpdateContactResponseFail } from './update-contact-response-fail';

describe('UpdateContactResponseFail', () => {
  it('should create an instance', () => {
    expect(new UpdateContactResponseFail()).toBeTruthy();
  });
});
