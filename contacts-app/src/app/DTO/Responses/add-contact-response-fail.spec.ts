import { AddContactResponseFail } from './add-contact-response-fail';

describe('AddContactResponseFail', () => {
  it('should create an instance', () => {
    expect(new AddContactResponseFail()).toBeTruthy();
  });
});
