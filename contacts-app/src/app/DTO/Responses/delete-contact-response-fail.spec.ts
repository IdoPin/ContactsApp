import { DeleteContactResponseFail } from './delete-contact-response-fail';

describe('DeleteContactResponseFail', () => {
  it('should create an instance', () => {
    expect(new DeleteContactResponseFail()).toBeTruthy();
  });
});
