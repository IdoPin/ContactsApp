import { DeleteGroupResponseFail } from './delete-group-response-fail';

describe('DeleteGroupResponseFail', () => {
  it('should create an instance', () => {
    expect(new DeleteGroupResponseFail()).toBeTruthy();
  });
});
