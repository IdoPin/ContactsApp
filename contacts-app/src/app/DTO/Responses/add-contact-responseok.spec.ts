import { AddContactResponseok } from './add-contact-responseok';

describe('AddContactResponseok', () => {
  it('should create an instance', () => {
    expect(new AddContactResponseok()).toBeTruthy();
  });
});
