import { UpdateGroupResponseFail } from './update-group-response-fail';

describe('UpdateGroupResponseFail', () => {
  it('should create an instance', () => {
    expect(new UpdateGroupResponseFail()).toBeTruthy();
  });
});
