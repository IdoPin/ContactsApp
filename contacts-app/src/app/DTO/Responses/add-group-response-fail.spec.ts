import { AddGroupResponseFail } from './add-group-response-fail';

describe('AddGroupResponseFail', () => {
  it('should create an instance', () => {
    expect(new AddGroupResponseFail()).toBeTruthy();
  });
});
